version: 2

models:
  - name: demo
    description: demo
    config:
      contract:
        enforced: true

    columns:
      - name: id
        description: This is a unique identifier for a customer
        data_type: integer
        constraints:
          - type: unique
          - type: not_null